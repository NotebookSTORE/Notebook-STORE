<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="prodBenefitModal"
            type="com.notebook.android.model.home.FreeDeliveryData.FreeDelivery" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/_4dp">

        <TextView
            android:id="@+id/tvFreeDeliveryTitle"
            android:text="@{prodBenefitModal.title}"
            android:layout_width="wrap_content"
            android:textSize="14sp"
            android:textColor="@color/colorPrimary"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <TextView
            android:id="@+id/tvFreeDeliverySubtitle"
            android:layout_width="0dp"
            android:textSize="14sp"
            android:layout_marginStart="8dp"
            android:textColor="@color/colorBlack"
            android:layout_height="wrap_content"
            app:layout_constraintLeft_toRightOf="@+id/tvFreeDeliveryTitle"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>